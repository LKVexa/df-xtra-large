# Security scope

Use trusted local source/output directories and separate production key provisioning. Included DEV_ONLY keys are public fixtures. The VM is a hosted reference process; do not treat it as an OS sandbox. The pure-Python signing fallback is not constant-time. See README.md and AUDIT.md for state-replay, filesystem-race and resource limitations. Report reproducible issues without uploading private keys or credentials.
