# Optimization Model

The optimization registry declares semantics-preserving passes and equivalence obligations. Every module includes an optimization-ready LCTL-C candidate. No performance superiority is claimed because domain behavior is not executable.
