# QUORUM Master Prompt & Workflow
## Promote All 315 RC-PW 7 PARTIAL Requirements to Fully Evidence-Backed OPERATIONAL

### Target repository

`QUORUM_GENERIC_PROJECT_LCTL_MSSL_VIRTUAL_MACHINE_5.0.0_CANDIDATE_RCPW_7.0.0_26_BLOCKERS_REMEDIATED.zip`

### Starting ledger

`OPERATIONAL=267 | PARTIAL=315 | BLOCKED=0 | REGRESSED=0`

### Mission

Using QUORUM, modify the **actual target VM/world repository**, execute the required qualification work, and drive the **315 exact PARTIAL requirement IDs in this package** to OPERATIONAL without regressing any of the already OPERATIONAL requirements.

The work must raise implementation fidelity where necessary; it is not sufficient to manufacture requirement-specific tests against behavior that remains a stub or bounded approximation.

## Global Workflow

### W0 — Immutable baseline
1. Extract the candidate to a clean working directory.
2. Hash all files and record tool/runtime/compiler/verifier versions.
3. Run the existing VM, RC-PW world, blocker-remediation, LCTL verification, headless verification, and qualification suites.
4. Freeze a baseline ledger and canonical digest set.
5. Fail immediately if any existing OPERATIONAL requirement regresses before modification.

### W1 — Requirement/evidence traceability
6. Import `PARTIAL_REQUIREMENTS.json` and `REMEDIATION_MATRIX.csv`.
7. Create a direct evidence-registry entry for every one of the 315 IDs.
8. Map each requirement to semantic contract, implementation path, tests, evidence path, profile, and acceptance predicate.
9. Prohibit one generic test from qualifying unrelated requirements unless it explicitly emits per-requirement assertions.

### W2 — Stage 01–06 foundations
10. Complete canonical authority, reference frames, fold geometry, LOD, persistence, and causal-ledger partials.
11. Add missing migrations, revisions, transactional boundaries, precision/property tests, conservation vectors, event schemas, replay and recovery.
12. Re-run Stages 01–06 plus all global regressions.

### W3 — Stage 07–09 living world
13. Complete ecology, civilization/economy, and narrative-gravity partials.
14. Add high-fidelity reference models, conservation comparisons, route-time/logistics tests, knowledge-locality, event reservations, bounded priority and deterministic remote outcomes.
15. Run multi-day deterministic living-world scenarios and replay them.

### W4 — Stage 10–15 embodiment and traversal
16. Complete materialization, rendering/visibility, save/replay, shell, no-unload and high-speed traversal partials.
17. Implement measurable readiness, temporal stability, presentation regression, portal/interior state, adaptive shells, transition cancellation, route-aware prewarm and teleport protocols.
18. Execute visual/reference regression, crash recovery, overload, oscillation and traversal torture suites.

### W5 — Stage 16–19 mathematics, folding, wells and history
19. Complete Penteract model, Fold Operator, authority-well and world-history partials.
20. Add dimension/unit validation, property tests, inverse targeting, deterministic well arbitration, archive proof classes, historical materialization provenance and query equivalence.
21. Compare single-worker and multi-worker semantics where required.

### W6 — Stage 20 integrated operational architecture
22. Complete all remaining Core Architecture + System Invariants partials.
23. Finish deterministic scheduler ordering, resource governor, compiler/runtime ABI, reproducible release manifests, compatibility matrix, rollback-tested release package and integrated scenario.
24. Make the release qualification consume the actual 315 requirement receipts.

### W7 — Fidelity/profile expansion
25. Execute every T2_HIGH_FIDELITY item at the fidelity it names, not merely as a contract.
26. Execute every T3_FABRIC item under actual spawned-worker/partitioned hosted conformance; if a requirement explicitly claims native/distributed production, run that actual target profile.
27. Execute T4_PRODUCTION_EXTERNAL items only in their required target/independent environment. Do not self-certify external independence.

### W8 — Golden World and autonomous-world closure
28. Map the 315 requirements to relevant GW-01…GW-80 scenarios.
29. Execute all Golden Worlds required by any still-PARTIAL requirement.
30. Add targeted Golden World variants where a requirement is not directly asserted by an existing scenario.
31. Run archive/rehydration, multi-era, frontier, knowledge-locality, generations, settlement/institution, ecology/economy, fabric and failure-recovery scenarios.

### W9 — Scale, soak and chaos
32. Declare exact scale/soak profiles before execution.
33. Run sustained content-scale world simulation with checkpoints, ledger compaction, worker faults/recovery, hot patch/rollback, fold/unfold, materialization storms, ecology/economy and replay.
34. Measure p50/p95/p99, memory/storage growth, backlog stability, canonical drift, identity leakage and recovery time.
35. Requirements mentioning soak/scale/capacity stay PARTIAL until the actual measured run completes.

### W10 — Independent/headless reproducibility
36. Build a fresh-extraction/headless qualification path using only packaged inputs and public runtime interfaces.
37. Reproduce canonical checkpoint and semantic-ledger digests.
38. For requirements explicitly requiring independent external qualification, execute a genuinely independent environment or preserve that profile as not yet promoted.

### W11 — Full regression and atomic promotion
39. Run every pre-existing test plus all new requirement-specific tests.
40. Re-run the LCTL canonical verifier and world compiler determinism.
41. Rebuild `RCPW_7_WORKFLOW_APPLICATION_LEDGER.json` from evidence registry predicates.
42. Require:
   - PARTIAL = **0**
   - BLOCKED = **0**
   - REGRESSED = **0**
   - OPERATIONAL = **582**
   only if all relevant profiles and evidence truly support those values.
43. Generate final source/evidence hashes, risk register, compatibility matrix, rollback report and release manifest.

## Final deliverables

- remediated VM repository ZIP;
- `315_PARTIAL_REMEDIATION_SUMMARY.json`;
- per-requirement evidence receipts;
- updated 582-item workflow ledger;
- stage qualification reports;
- full regression logs;
- Golden World matrix;
- performance/soak reports;
- multi-worker/fabric conformance;
- headless replay digests;
- compatibility/migration/rollback evidence;
- `FINAL_OPERATIONAL_REPORT.md`;
- SHA-256 release manifest.

## Absolute completion rule

**Do not report 582/582 OPERATIONAL unless all 315 original PARTIAL IDs are supported by fresh executable evidence at their claimed qualification profiles.**
