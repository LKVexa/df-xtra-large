ja source 0.3
module suite56.job_queue_workers_sandboxes.worker_management
use Operations
policy explicit_network

workspace WorkerManagement {
    target windows-x64
    compiler profile release
    dependencies locked
    environment offline
    resource cpu 8 memory 16GB
    network disabled
    secret worker_key reference "vault://suite56/worker-management"

    service SOPHIA_worker_context replicas 1
    health_check SOPHIA_worker_context every 10s
    service CHARLOTTE_worker_gate replicas 1
    health_check CHARLOTTE_worker_gate every 10s
    service LANDON_worker_controller replicas 1
    health_check LANDON_worker_controller every 10s
    service Professor_worker_report replicas 1
    health_check Professor_worker_report every 10s
    service Podium_worker_receipt replicas 1
    health_check Podium_worker_receipt every 10s
    service worker_management_worker replicas 1
    health_check worker_management_worker every 5s

    upgrade rolling
    rollback on health_failure
    package msslb
}

assert deployment.reproducible == true
emit package "job_queue_workers_worker_management.msslb"
