# Security Model

Default deny; network deny; unknown-code deny. Capabilities are explicit semantic declarations and do not themselves grant host privileges. MSSL tamper checks are evidenced. Production penetration/sandbox qualification remains blocked until domain runtimes exist.
