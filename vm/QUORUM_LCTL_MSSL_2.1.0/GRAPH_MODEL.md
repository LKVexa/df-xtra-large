# Graph Model

Each module has stable nodes, sequential typed control/data edges, policy gates, state maps and lowering traceability. Repository-wide hard dependencies are not invented; scenario plane edges are labeled as scenario handoffs.
