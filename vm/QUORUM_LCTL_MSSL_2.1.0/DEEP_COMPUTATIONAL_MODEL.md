# Deep Computational Model

The repository now expresses domain-correct staged computation with stable graph nodes, canonical operators, typed handoffs, default-deny capability policy, stage evidence and replay pins. Legal LCTL `REGION` and `DEPENDENCY` rows carry the generic execution topology; the terminal NOP is only a justified canonical entry/end sentinel. Domain-specific native effects remain explicit blockers rather than being simulated dishonestly.
