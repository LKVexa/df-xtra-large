# NOT A CERTIFICATE — L4–L9 Evidence Template

This file is deliberately **NOT VALID AS CERTIFICATION**. It documents the remaining evidence required before any L4–L9 PASS can be issued.

- L4: module-specific native operators must consume positive/negative/edge fixtures and produce contract-defined domain outputs.
- L5: execute legacy and new runtimes on equivalent fixtures and compare outputs/state/effects.
- L6: replay the domain execution state and outputs exactly or within declared tolerance.
- L7: execute domain-specific security negative tests and capability boundaries.
- L8: benchmark real domain workloads with declared hardware/runtime/environment and acceptance thresholds.
- L9: pass all applicable prior gates, end-to-end integration/soak/recovery/release checks, and external physical/runtime authority where required.
