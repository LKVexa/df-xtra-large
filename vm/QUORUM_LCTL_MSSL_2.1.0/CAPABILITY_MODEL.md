# Capability Model

`registry/CAPABILITY_GRAPH.json` defines the shared capability vocabulary; each module declares the minimum candidate capabilities in `graph/policy_map.json`. A future runtime must enforce grants explicitly and emit receipts.
