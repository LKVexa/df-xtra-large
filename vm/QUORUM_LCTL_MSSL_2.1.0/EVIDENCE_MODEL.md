# Evidence Model

Authority, LCTL source, canonical LCTL, fixtures, state transitions, replay and qualification are hash-bound. Contract-level execution and security receipts are emitted by the reference qualification layer.
